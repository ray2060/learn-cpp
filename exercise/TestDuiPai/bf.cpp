#include <iostream>

using namespace std;

int main() {
    freopen("data.in", "r", stdin);
    freopen("data.out", "w", stdout);
    int a, b;
    cin >> a >> b;
    cout << (a + b) << endl;
    return 0;
}